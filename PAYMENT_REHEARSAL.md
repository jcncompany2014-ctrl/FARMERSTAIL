# 결제 전체 리허설 — 테스트키로 실전 배선 검증 (2026-07-29)

> ## ⚠️ 먼저: "심사 중이라 테스트도 못 한다"는 오해입니다
>
> **토스 심사가 막는 건 운영키(진짜 돈)뿐입니다.** 테스트키는 개발자 계정만
> 있으면 심사와 무관하게 바로 쓸 수 있고, **이 프로젝트엔 이미 들어 있습니다.**
>
> 2026-07-29 실측 확인:
> - `.env.local` 의 `TOSS_SECRET_KEY` · `NEXT_PUBLIC_TOSS_CLIENT_KEY` 둘 다
>   `test_` 접두사 (실과금 위험 0)
> - 그 시크릿키로 토스 API 를 실제 호출 → `HTTP 404 NOT_FOUND_PAYMENT`
>   = **"키는 유효한데 그 결제건이 없다"** → 인증 통과, 테스트 모드 사용 가능
> - 프로덕션(`/api/health`) → `toss: true` = 운영 배포에도 키 두 개가 설정됨
>   (어느 모드인지는 관리자 홈 배너가 알려줍니다 — 0단계)
>
> **왜 지금 하나**: 테스트키로 결제창 → 승인 → 주문 → 구독 → 자동결제 → 환불
> **전체 사슬을 오늘 돌려볼 수 있습니다.** 이 앱은 결제 성공이 아직 0건이고,
> 방금 감사에서 나온 청구액 조작 취약점도 "한 번도 안 흘러본 물길"이라 생긴
> 것입니다. 물을 한 번 흘려보는 것이 남은 버그를 찾는 가장 확실한 방법입니다.
>
> 사장님이 폰으로 따라 하는 부분(카드 입력 등)과 제(Claude)가 하는 부분이
> 나뉘어 있습니다. **카드 정보 입력은 제 안전 규칙상 항상 사장님 몫**입니다 —
> 테스트키라 실제 과금은 없지만 규칙에 예외가 없습니다.

---

## ⚠️ Vercel 크론 한도 — 배포를 막는 함정 (2026-07-30 실제로 겪음)

**환불 재시도 크론을 하루 1회 → 매시간으로 바꿨더니 Vercel 이 배포 자체를
거부했습니다.** 빌드 실패가 아니라 **빌드 시작을 거부** — Vercel 에 배포 기록이
아예 안 생기고, GitHub 커밋에만 `Vercel — Deployment failed.` 이 붙습니다.
그 링크가 크론 요금 문서를 가리켜서 원인을 찾았습니다.

증상이 헷갈립니다: 프로덕션은 옛 배포로 **계속 잘 돌아가서** 아무 문제 없어
보입니다. 그 사이 올린 커밋 전부가 조용히 반영되지 않습니다.

**규칙: `vercel.json` 의 크론은 전부 하루 1회 이하로 유지한다.**
(시 자리가 `*` · 콤마 목록 · `*/N` 이면 하루 1회를 넘긴다)

**Pro 업그레이드 후 되돌릴 것 — 지금 하루 1회로 눌러둔 것들:**

| 크론 | 지금 | 원하는 값 | 왜 |
|---|---|---|---|
| `/api/cron/refund-retry` | `30 19 * * *` | `30 * * * *` | 환불 재시도가 하루 1회면 느리다(결제 감사 #6) |
| `/api/cron/tracking-poll` | `30 9 * * *` | `*/30 * * * *` | 하루 1회면 저녁 이후 도착분의 "배송 완료" 알림이 다음 날로 밀린다 |

**2026-07-30 — 알림 크론 4개를 조용시간 밖으로 옮겼습니다.**
푸시가 한 건도 나가지 않던 버그(`lib/push.ts` 가 쿠키 클라이언트를 써서 크론에서
모든 조회가 0행)를 고치는 순간, 조용시간(기본 22–08)에 예약된 크론은 그 즉시
**정당하게 차단**됩니다("고객이 껐으니 안 보낸 것"). 그래서 둘을 같이 옮겼습니다:

| 크론 | 이전 (KST) | 지금 (KST) | 이유 |
|---|---|---|---|
| `subscription-charge` | 04:00 | **09:10** | 실패를 당일 업무시간에 처리. 포장 시작 전 |
| `personalization-approval-timeout` | 03:00 | **08:40** | charge 보다 먼저라는 순서 유지 |
| `personalization-progression` | 05:00 | **10:10** | charge 다음 1시간 간격 유지 |
| `tracking-poll` | 01:00 | **18:30** | 당일 배송이 끝난 뒤 |

`lib/audit-rules.test.ts` 의 규칙9 가 이걸 지킵니다 — 푸시를 보내는 크론이 KST
22–08 에 들어가면 테스트가 깨집니다.

배포가 안 올라가는 것 같으면 **가장 먼저** 이걸 확인하세요:

```bash
node -e "const c=require('./vercel.json').crons;const o=c.filter(x=>{const p=x.schedule.split(' ');return p[1]==='*'||p[1].includes(',')||p[1].includes('/')});console.log(o.length?o:'전부 하루 1회 이하 OK')"
```

---

## 0. 사전 확인 (1분)

1. 폰에서 **관리자 홈**을 엽니다.
2. 맨 위 배너가 **"⚠️ 테스트 모드예요"** 인지 확인합니다.
   - "✅ 실결제 모드예요"가 보이면 **여기서 중단** — 리허설이 진짜 돈을 씁니다.
   - "🚨 결제 키가 서로 짝이 안 맞아요"가 보이면 그것부터 알려주세요.
   - **이 배너 사진 한 장만 보내주셔도 제가 판단해 드립니다** (관리자 화면은
     제가 로그인할 수 없어서 직접 볼 수 없습니다).

> 카드 입력 단계에서는 토스가 제공하는 **테스트 카드번호**를 쓰면 본인 카드를
> 안 넣어도 됩니다. 토스 개발자센터 → 결제위젯 → 테스트 카드번호 문서에
> 있습니다(예: `4330-1234-1234-1234` 형태). 실카드를 쓰셔도 테스트키라
> 과금은 없지만, 테스트 카드가 더 안전합니다.
3. 리허설용 계정을 하나 정합니다 — **본인 부계정 이메일** 추천
   (탈퇴 리허설까지 하므로 주계정은 쓰지 마세요).

## 1. 가입 → 구독 신청 (폰, ~7분)

| # | 하실 것 | 확인할 것 |
|---|---|---|
| 1 | 시크릿 창에서 `www.farmerstail.kr/start` → 설문 완료 (알레르기 **'닭' 하나 선택**) | 결과 화면 단백질 후보에 **닭이 없어야** 함 |
| 2 | 결과 화면에서 이메일 가입(부계정) → 메일 인증 → 로그인 | 강아지·설문이 계정에 붙어 있는지 (앱 홈에 강아지 카드) |
| 3 | 분석 화면 → 플랜 → 구독 신청 진행 | **닭 레시피가 잠겨 있는지** ← 이번에 고친 critical 검증 |
| 4 | 결제수단 선택 화면에서 **신용·체크카드** → 본인 카드 입력 | 테스트키라 **실제 승인·과금 없음**. 등록 후 앱 홈에 다음 배송일(화요일) 표시 |

> 4번이 이 리허설의 첫 관문입니다 — billing-auth 경로는 한 번도 실행된 적이
> 없습니다. 여기서 에러가 나면 화면 그대로 스크린샷 주세요.
>
> **리허설은 카드로 완주하세요.** 토스페이는 아래 별도 섹션에서 다룹니다 —
> 카드는 어떤 고객이든 쓸 수 있어야 하는 필수 경로이고, 이번 감사에서 나온
> 청구액·주문 연결 문제도 카드 경로로 검증됩니다.

## 1-B. 토스페이 자동결제 — ⛔ 폐기됨 (2026-08-11)

> **이 섹션은 더 이상 유효하지 않습니다.** 토스가 "자동결제(빌링)는 카드 등록
> 전용 — 계좌이체·간편결제 미지원"이라고 확정(2026-08-11 사장님 확인). 그래서
> 토스페이를 제공 목록(`lib/payments/billing-methods.ts` 의 `ALL`)에서 제거하고
> 플래그를 영구 껐습니다. 빌링은 **카드 전용**이며, 재노출 금지는 audit 규칙23
> 이 런타임으로 못박습니다. 아래 내용은 당시 기록으로만 보존합니다.

사장님 지시로 **카드 / 토스페이** 두 수단을 붙였습니다(네이버페이 제외).
카드번호를 안 치고 **토스에 이미 등록된 수단에서 고르는** 경로입니다.

### ⛔ 출시 전 반드시 확인: 토스페이 노출

> **목표 상태는 '켜짐'입니다.** 토스 PG 심사가 끝나면 토스페이 자동결제도 함께
> 열리는 것이 정상 경로이고, 지금 켜 둔 것(2026-07-31 사장님 지시 — "걍 켜,
> 어차피 실 고객들이 보기 전까지는 나만 계속 보면서 운영할 거니까")은 그 상태로
> 가는 길입니다.
>
> **다만 지금은 아직 상점 설정 전이라 누르면 실패합니다.** 사장님만 보는 지금은
> 괜찮지만 실 고객이 그 상태를 만나면 안 됩니다.
>
> **출시 전에 할 일은 끄는 게 아니라 눌러 보는 것입니다:**
> 폰으로 토스페이 등록을 1회 해서 **창이 열리고 어드민에 '토스페이' 로 뜨는지**
> 확인하세요(카드번호가 없는 수단이라 이 표시가 정상 동작의 증거입니다).
>
> - ✅ 되면 → 그대로 둡니다. 끝.
> - ⚠️ 토스가 끝내 안 켜 줬으면 → **그때만** Vercel 환경변수
>   `NEXT_PUBLIC_TOSSPAY_BILLING=off` → 재배포. 이건 **비상구**지 계획이 아닙니다.
>
> (`lib/audit-rules.test.ts` 규칙23 이 이 문단과 코드 기본값을 묶어 둡니다 —
>  한쪽만 지우면 테스트가 깨져서, 확인 자체를 잊는 걸 막습니다.)

### 배경 — 왜 한때 꺼뒀었나 (2026-07-30)

**2026-07-30 실기기 시도에서 토스가 이렇게 답했습니다:**

> **"토스페이 자동결제 결제수단 설정이 없습니다"**

계약·신청과 **별개로**, 토스페이먼츠 상점 설정에 **토스페이 자동결제 수단이
등록**되어 있어야 창이 열립니다. 그 전에 노출하면 고객이 토스페이를 누르는
순간 실패합니다 — **작동하지 않는 걸 보여주는 쪽이 훨씬 나쁘므로** 껐습니다.

| | 상태 |
|---|---|
| 코드 | ✅ 완료 (`lib/payments/billing-methods.ts` 정본, 테스트 16) |
| 화면 | ✅ 선택 버튼 = 곧바로 토스 창 (확인 화면 없음) |
| 토스 샌드박스 | ✅ 창은 열림 |
| **운영(실기기)** | ❌ **"자동결제 결제수단 설정이 없습니다"** |
| 노출 | ⛔ 꺼둠 (기본 꺼짐) |

### 토스에 요청할 것

심사 문의와 **별개 항목**입니다. 이렇게 물어보세요:

> "상점에 **토스페이 자동결제(빌링)** 결제수단을 등록해 주세요.
>  현재 `requestBillingAuth` 에 `flowMode: DIRECT` · `easyPay: TOSSPAY` 로
>  호출하면 '토스페이 자동결제 결제수단 설정이 없습니다' 로 거절됩니다."

### 켜는 절차 (토스가 설정해 준 뒤)

1. Vercel → Settings → Environment Variables 에
   `NEXT_PUBLIC_TOSSPAY_BILLING` = `on` 추가 → **재배포**
   (빌드 시점에 박히는 값이라 재배포 필요)
2. 폰으로 토스페이 등록 1회 → 관리자에서 **결제수단이 '토스페이' 로 뜨는지**
   확인 (카드번호가 없는 수단이라 이 표시가 정상 동작의 증거)

코드 수정은 필요 없습니다 — 환경변수 한 줄이면 켜집니다.

### 화면 흐름

**주문 화면(배송지)** — 새 신청은 여기서 끝난다.
```
배송지 입력
결제수단   [신용·체크카드]  [토스페이]     ← 토스페이는 토스 파란색
      [ 신청하기 ]  →  곧바로 토스 창
```

**등록 화면(`/subscribe/billing-auth`)** — 재등록·마이페이지 진입용.
```
[결제하기]  ← 우리가 그린 헤더
결제수단을 골라주세요
  ┌ 신용·체크카드 ────────┐
  └ 토스페이 (파란 버튼) ──┘   ← 누르면 바로 토스 창
```

**확인 화면을 없앴습니다** (사장님 2026-07-30). 전엔 고른 뒤 "○○로 등록하려면
다음을 눌러주세요" 를 한 번 더 띄웠는데, 그 문구가 **토스 자체 화면의 문구와
거의 같아서** 우리 화면을 토스 화면으로 착각하게 만들었습니다("우리 앱 컬러로
나와버리는데 왜이래"). 남의 화면을 흉내내면 안 됩니다. 게다가 토스페이는 토스가
같은 안내를 이미 띄우니 '다음'을 두 번 누르게 됩니다.

**버튼을 누르는 것 자체가 사용자 제스처**라, 확인 화면 없이 바로 열어도 iOS
이동 차단에 걸리지 않습니다. (수단이 하나뿐이거나 `?method=` 로 지정돼 들어온
경우엔 누를 대상이 없으므로 '등록하기' 버튼 하나를 둡니다 — 문구는 우리 말투로.)

### 주소창(도메인 표시)에 대해 — 우리가 끌 수 없는 부분

우리 화면에는 **우리가 그린 헤더**가 있어 브라우저처럼 보이지 않습니다.
하지만 **토스 화면으로 넘어가면 iOS 가 도메인을 표시합니다.** 이건 다른 회사
결제 페이지로 넘어갈 때 iOS 가 붙이는 **피싱 방지 장치**라 웹에서는 끌 수
없습니다. (고객 입장에선 "토스 화면이 맞다"는 확인이라 나쁜 것만은 아닙니다.)

완전히 없애려면 **네이티브 앱에서 웹뷰를 직접 띄워야** 합니다 — 배민이 그렇게
하고 있습니다(사장님 스크린샷 IMG_4757 의 '결제하기' 헤더). 네이티브 프로젝트를
만든 뒤 할 일이라 NATIVE_APP_SETUP.md 에 적어뒀습니다.

### 리허설에서 남은 확인 (실기기)

| # | 확인할 것 | 왜 |
|---|---|---|
| 1 | 폰에서 토스페이 등록 1회 완주 | 샌드박스에서 창이 열린 것까지만 확인됐습니다 |
| 2 | 등록 후 화면에 **'토스페이'** 로 표시되는지 | 카드번호가 없는 수단이라 이 표시가 정상 동작의 증거입니다 |
| 3 | 홈 화면에 설치한 앱(PWA)에서 등록 후 **앱으로 돌아오는지** | 토스에 다녀온 뒤 앱으로 안 돌아오면 등록은 됐는데 화면이 모르는 상태가 됩니다. 안 돌아오면 알려주세요 |
| 4 | 구독 관리에서 '재개'가 정상 동작하는지 | 예전엔 카드번호로 등록 여부를 봤습니다(고쳤음) |

## 2. 자동결제 발화 (저와 함께, ~5분)

카드 등록 직후 상태: `next_delivery_date = 다음 화요일`. 자동결제는 그날 새벽에
돌므로, 리허설에선 날짜를 오늘로 당겨서 발화시킵니다.

1. **저한테 "리허설 결제 당겨줘"라고 말씀해 주세요** — 제가 그 구독의
   `next_delivery_date`를 오늘로 바꿉니다(리허설 계정만).
2. 크론 발화는 둘 중 하나:
   - **기다리기**: 다음 새벽 04시(KST) 자동 실행
   - **즉시**: Vercel 대시보드 → 프로젝트 → Settings → Environment Variables
     에서 `CRON_SECRET` 값을 복사해 저한테 실행을 요청하시거나, 직접:
     ```bash
     curl -H "Authorization: Bearer {CRON_SECRET값}" https://www.farmerstail.kr/api/cron/subscription-charge
     ```
3. 확인할 것 (관리자 페이지):
   - 주문 관리에 새 주문 1건 (결제 완료)
   - 자동결제 이력에 성공 1건
   - **도장판에 도장 1개** ← 이번에 고친 critical(#1) 검증
   - 앱 홈 배송일이 +2주로 밀렸는지 · **화요일인지** ← critical(#3) 검증
   - 피킹 리스트(다음 화요일)에 이 박스가 뜨는지

## 3. 환불 리허설 (폰, ~3분)

1. 관리자 → 주문 관리 → 방금 주문 → **부분 취소**로 일부 금액 환불
2. 확인: 환불 내역 페이지에 기록 · 잔액 초과 환불이 **거부**되는지
   (전액보다 큰 금액을 넣어보세요 — 막혀야 정상)

## 4. 실패 경로 리허설 (선택, +5분)

토스 테스트 환경은 특정 카드번호로 실패를 시뮬레이션할 수 있습니다
(토스 개발자센터 → 테스트 카드번호 문서). 잔액부족 카드로 등록해서:
- 결제 실패 → 앱에 "결제 실패" 안내가 오는지
- 다음날 재시도가 **새 멱등키**로 나가는지 ← 이번에 고친 critical(#2)은
  로그로 제가 확인해 드립니다

## 5. 탈퇴 리허설 (폰, ~2분)

리허설 계정으로: 마이페이지 → 회원 탈퇴
- 확인(제가 DB로): 구독 cancelled + 카드토큰 말소 ← 이번에 고친 #9 검증
- 확인(사장님): 탈퇴 후 로그인이 안 되는지

---

## 성공 기준

위 5단계가 전부 통과하면 — **결제 사슬 전체가 실행 검증된 것**입니다.
심사 통과 후 남는 건 둘뿐입니다:
1. Vercel 환경변수를 운영키(`live_`)로 교체 → 관리자 배너가
   "실결제 모드"로 바뀌는 것 확인
2. 사장님 본인 카드로 **소액 실결제 1회** (첫 진짜 거래)

## 기록

| 날짜 | 단계 | 결과 | 메모 |
|---|---|---|---|
| | | | |
