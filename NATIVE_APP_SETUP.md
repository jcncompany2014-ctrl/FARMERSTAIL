# 네이티브 앱(iOS · Android) 시작 가이드 — 2026-07-26

> 이 문서는 **맥에서 따라 하는 순서서**입니다. 코드 쪽 사전 준비는 끝났고,
> 여기서부터는 애플·구글 계정과 Xcode가 있어야 하는 일들입니다.
> 막히면 그 단계 번호를 알려주세요.

---

## 지금 상태

| | 상태 |
|---|---|
| Capacitor 패키지 (iOS·Android·푸시·공유·스플래시) | ✅ 설치됨 |
| 앱 ID | ✅ `com.farmerstail.app` — **한 번 정하면 못 바꿉니다** |
| 앱 이름 | ✅ 파머스테일 |
| Apple 로그인 코드 | ✅ 있음 (애플이 필수로 요구 — 가이드라인 4.8) |
| 앱 아이콘·스플래시 원본 | ✅ `assets/` 에 생성됨 (아래 2단계에서 자동 변환) |
| `ios/` · `android/` 프로젝트 폴더 | ⬜ 아직 없음 — 1단계에서 생성 |
| 딥링크(알림 눌러 특정 화면 열기) | ⬜ 3단계 |
| 푸시 인증서 | ⬜ 4단계 |

## 앱 구조 — 먼저 이해하고 가기

이 앱은 **`www.farmerstail.kr` 을 감싼 껍데기**입니다(`capacitor.config.ts`).
그래서:

- **좋은 점**: 화면·기능을 고쳐서 Vercel 에 배포하면 **스토어 재심사 없이 즉시
  반영**됩니다. 앱을 껐다 켜면 새 버전입니다.
- **주의할 점**: 네이티브 쪽(푸시 권한 문구, 플러그인 추가, 아이콘)을 바꾸면
  그때는 **재심사**가 필요합니다.
- **약점**: 네트워크가 없으면 아무것도 안 뜹니다(흰 화면). → 아래 '나중에' 참고.

---

## 1단계 — 네이티브 프로젝트 만들기 (맥)

```bash
cd ~/farmerstail-app && git pull
npm install
npx cap add ios
npx cap add android
```

`ios/` 와 `android/` 폴더가 생깁니다. **이 폴더도 git 에 커밋합니다**(Capacitor 권장).

> 안드로이드는 Windows 에서도 되지만, 맥에서 둘 다 하는 편이 헷갈리지 않습니다.

## 2단계 — 아이콘·스플래시 자동 생성 (맥)

원본은 이미 `assets/` 에 만들어 뒀습니다. 한 줄이면 iOS·Android 의 모든 크기가
자동으로 채워집니다.

```bash
npx @capacitor/assets generate --iconBackgroundColor '#A0452E' --splashBackgroundColor '#F5F0E6'
```

원본을 다시 만들어야 하면(로고가 바뀌면) `node scripts/generate-native-assets.mjs`
를 먼저 돌리세요. 그 스크립트는 **iOS 아이콘에 투명도가 없는지 검사**합니다 —
투명도가 있으면 앱스토어가 거절하기 때문에 일부러 실패시킵니다.

## 3단계 — 애플 개발자 설정 (developer.apple.com)

1. **Apple Developer Program 가입** — 연 $99. 결제 후 승인까지 하루 이틀 걸립니다.
2. **App ID 등록** — `com.farmerstail.app`
3. **Sign in with Apple 체크** — 카카오 로그인을 제공하므로 애플이 **필수로**
   요구합니다(가이드라인 4.8). 빠뜨리면 무조건 거절입니다.
4. **Services ID** 생성 — 웹에서 애플 로그인을 쓰기 때문에 필요합니다.
   - 식별자: `com.farmerstail.app.signin`
   - Return URL: `https://www.farmerstail.kr/auth/callback`
5. **Sign in with Apple 키(.p8) 발급** → Supabase 대시보드 →
   Authentication → Providers → Apple 에 등록

## 4단계 — 푸시 알림

앱 안에서 이미 웹 푸시를 쓰고 있지만, **네이티브 푸시는 별도 설정**입니다.

- **iOS**: Apple Developer → Keys → APNs 키(.p8) 발급 → Xcode 에서
  Push Notifications capability 켜기
- **Android**: Firebase 프로젝트 생성 → `google-services.json` 을
  `android/app/` 에 넣기

> 푸시는 애플 심사에서 "이 앱은 웹사이트가 아니다"라는 **가장 강력한 근거**입니다.
> 꼭 동작하는 상태로 제출하세요.

## 5단계 — 딥링크 (알림 눌러서 특정 화면 열기)

서버 쪽 파일 두 개가 필요한데, **채워 넣을 값을 아직 모릅니다**(3단계에서 나옵니다).

- `public/.well-known/apple-app-site-association` — 애플 팀 ID 필요
- `public/.well-known/assetlinks.json` — 안드로이드 앱 서명 지문(SHA-256) 필요

3단계·6단계를 마치고 그 값을 알려주시면 제가 만들어 드리겠습니다.

## 6단계 — 스토어 등록

**App Store Connect**
- 앱 생성 → 이름·부제·설명·키워드
- 스크린샷 (6.7형 필수 / 6.5형 권장)
- **개인정보 라벨** — 수집 항목을 정확히 적어야 합니다(이메일·이름·강아지 정보·
  결제. 결제는 토스가 처리하므로 카드번호는 우리가 저장하지 않음)
- 개인정보처리방침 URL: `https://www.farmerstail.kr/legal/privacy`
- **계정 삭제 경로** 필수: `https://www.farmerstail.kr/mypage/delete` ✅ 이미 있음

**Google Play Console**
- 등록비 $25 (1회)
- 데이터 세이프티 폼 (애플 개인정보 라벨과 같은 내용)
- 앱 서명 키 등록 → SHA-256 지문을 5단계에 사용

---

## 심사에서 걸릴 수 있는 것 (미리 알고 가기)

**가이드라인 4.2 — 최소 기능.** "웹사이트를 그냥 감싼 앱"은 거절 사유입니다.
우리는 푸시 알림·스플래시·공유가 있어서 방어할 근거가 있습니다.

거절당해도 큰일이 아닙니다 — 사유를 알려주므로 보완해서 재제출하면 보통 며칠
안에 통과합니다. **일단 제출해서 애플이 실제로 뭘 문제 삼는지 보는 편**이
빠릅니다.

거절 시 보완 카드(순서대로):
1. 오프라인 화면 — 네트워크가 없어도 최근 정보(다음 배송일·우리 아이)를 보여주기
2. 카메라로 강아지 사진 바로 찍기 (네이티브 기능)
3. 홈 화면 위젯

## 나중에 (심사와 별개로 필요한 것)

**오프라인 화면**은 심사 통과 여부와 무관하게 필요합니다. 지금은 지하철·엘리베이터
에서 앱을 열면 **흰 화면**이 뜹니다. 고객이 보면 "앱이 고장났다"고 생각하고 지웁니다.

---

## 순서 요약

```
3단계 애플 가입 (승인 1~2일)  ─┐
                              ├→ 4·5단계 → 6단계 제출 → 심사(1~3일)
1·2단계 프로젝트·아이콘 (30분) ─┘
```

애플 가입 승인이 제일 오래 걸리니 **3단계를 제일 먼저** 시작하세요.
그 사이에 1·2단계를 하시면 됩니다.
